<i class="fa-solid fa-@(Icon) fa-@(Animation)"></i>

@code {
    [Parameter] public string Icon { get; set; } = string.Empty;
    [Parameter] public string Animation { get; set; } = string.Empty;
}
